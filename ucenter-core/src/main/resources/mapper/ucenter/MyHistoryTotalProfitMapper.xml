<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mzwise.modules.ucenter.mapper.UcHistoryTotalProfitMapper">
    <select id="calMyTeamHistoryTotalProfit" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(uhtp.total_community_profit), 0)
        FROM uc_history_total_profit AS uhtp
        WHERE member_id IN #{memberIdList}
    </select>

    <select id="calMyTeamBTEHistoryTotalProfit" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(uhtp.total_bte_community_profit), 0)
        FROM uc_history_total_profit AS uhtp
        WHERE member_id IN #{memberIdList}
    </select>

</mapper>