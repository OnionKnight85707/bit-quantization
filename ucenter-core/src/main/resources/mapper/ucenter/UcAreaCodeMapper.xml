<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mzwise.modules.ucenter.mapper.UcAreaCodeMapper">

    <select id="areaCodelist" resultType="com.mzwise.modules.ucenter.vo.AreaCodeVO">
        select area_code, region from uc_area_code where language = #{language} order by major_national desc, area_code asc
    </select>

    <select id="selectByAreaCode" parameterType="java.lang.String" resultType="com.mzwise.modules.ucenter.entity.UcAreaCode">
        select * from uc_area_code where area_code = #{areaCode} and language = #{language}
    </select>


</mapper>